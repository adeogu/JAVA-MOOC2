class numofnegatives{
  public static void main(string[]args){
    Scanner scanner = new Scanner(system.in);
    int count = 0;
    while(true){
      int value = scanner.nextInt();
      if(value==0){
        break;
      }else if (values< 0){
        i++
      }
      system.out.println("number of negatives" + count);
    }
    scanner.close();
    
  }
}
